{
	"name": "efobi-dev",
	"dependencies": {},
	"scripts": {
		"build": "cd astro && bun install && bun run build",
		"format": "biome check --write"
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.4"
	}
}
